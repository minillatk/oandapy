language: python
python:
  - 3.4
  - 3.5
  - 3.6
install:
  - pip install -r requirements-dev.txt
  - pip install coveralls
script:
  pytest -vv -s
after_success:
  coveralls
git:
  submodules: false

