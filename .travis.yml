language: python
python:
  - 3.4
  - 3.5
  - 3.6
  - 3.7
install:
  - pip install -r requirements-dev.txt
  - pip install coveralls
script:
#  coverage run --source=oandapy setup.py test
  pytest -vv -s
after_success:
  coveralls
